char q(double p, char **nums, int n, int m) {
    vector<int> v(256, 0);
    for (int i = 0; nums[i]; i++)
        for (int j = 0; nums[i][j]; j++) {
            v[nums[i][j]] += 1;
        }

}

//
double temp;

v

for (int i = 255; i >= 0; i--)
{
temp += v[i] / (n * m);
}
return '0';